# Architecture Artifacts

Store executable or implementation-specific architecture artifacts here when they do not belong with a component. Canonical architecture narratives and diagrams remain in `docs/en/architecture/` and `diagrams/`.
