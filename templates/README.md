# Templates

Reusable source templates belong here. The current ADR template is canonically stored at `adr/ADR-template.md`; use it for architecture decisions until a migration is recorded in an ADR.
