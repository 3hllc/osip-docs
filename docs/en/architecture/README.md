# Architecture

This section describes the platform before implementation. It contains the system models, cross-cutting concerns, and specifications that constrain software, hardware, cloud, edge, and installer work.

The [Zigbee Mesh and MQTT Bridge](zigbee-mesh-and-mqtt-bridge.md) specification separates the physical Zigbee device mesh from OSIP's MQTT event backbone and defines their adapter boundary.

The [Zigbee Mesh and MQTT Bridge](zigbee-mesh-and-mqtt-bridge.md) specification separates the physical Zigbee device mesh from OSIP's MQTT event backbone and defines their adapter boundary.
