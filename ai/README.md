# AI Components

Store implementation artifacts for AI capabilities here: prompts, model adapters, evaluation fixtures, and runtime configuration. Canonical AI architecture and product decisions remain in `docs/en/ai/` and ADRs.
